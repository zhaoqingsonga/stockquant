% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_stock.R
\name{stock_prediction_hw}
\alias{stock_prediction_hw}
\title{股票价格预测函数（使用 Holt-Winters 模型）}
\usage{
stock_prediction_hw(
  stock_data = stock_data_ss50[["600048.SS"]],
  prediction_period = 30,
  frequency = NULL,
  alpha = NULL,
  beta = NULL,
  gamma = NULL
)
}
\arguments{
\item{stock_data}{包含股票数据的 xts 对象。}

\item{prediction_period}{预测周期，默认为 30 天。}

\item{frequency}{时间序列频率。如果为 NULL，则尝试自动检测。}

\item{alpha}{Holt-Winters 模型的 alpha 参数，默认为 NULL，表示自动选择。}

\item{beta}{Holt-Winters 模型的 beta 参数，默认为 NULL，表示自动选择。}

\item{gamma}{Holt-Winters 模型的 gamma 参数，默认为 NULL，表示自动选择。}
}
\value{
返回一个 `xts` 对象，包含预测的未来日期和对应的股票价格。

包含预测价格的 xts 对象。
}
\description{
该函数用于对给定的股票数据进行价格预测，主要使用 Holt-Winters 模型。
如果 Holt-Winters 模型拟合失败，将自动改用 ARIMA 模型进行预测。
}
\examples{
\dontrun{
# 假设 stock_data_ss50 是已经存在的股票数据集
result <- stock_prediction_hw(stock_data = stock_data_ss50[["600048.SS"]], prediction_period = 30)
print(result)
}

使用 Holt-Winters 或 ARIMA 模型预测股票价格

该函数使用 Holt-Winters 或 ARIMA 模型对股票数据进行预测。
如果 Holt-Winters 模型拟合失败，则尝试使用 ARIMA 模型。

# 假设 stock_data_ss50 是包含股票数据的 xts 对象
predictions <- stock_prediction_hw(stock_data = stock_data_ss50[["600048.SS"]], prediction_period = 60)
print(predictions)
}
